@model WebApplicationMArt.Models.Administrateur

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Administrateur</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdAd" />
            <div class="form-group">
                <label asp-for="LoginAd" class="control-label"></label>
                <input asp-for="LoginAd" class="form-control" />
                <span asp-validation-for="LoginAd" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PassAd" class="control-label"></label>
                <input asp-for="PassAd" class="form-control" />
                <span asp-validation-for="PassAd" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
