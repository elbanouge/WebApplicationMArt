@model IEnumerable<WebApplicationMArt.Models.Administrateur>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LoginAd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PassAd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LoginAd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PassAd)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAd">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAd">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAd">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
